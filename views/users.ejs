<!DOCTYPE html>
<html lang="en">

<head>
	<link rel="stylesheet" href="styles.css">
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Your Forum App - Users</title>
</head>

<body>
	<h1>Registered Users</h1>

	<% if (loggedInUser) { %>
		<p>Welcome, <%= loggedInUser.username %>! <a href="/logout">Logout</a></p>
		<% } else { %>
			<a href="/register">Register to add ur name to the list</a>
			<br>
			<br>
			<a href="/login">Login</a>
			<br>

			<% } %>

				<ul>
					<% users.forEach(user=> { %>
						<li>
							<%= user.username %>
						</li>
						<% }); %>
				</ul>

				<a href="/">Back to Home</a>
</body>

</html>